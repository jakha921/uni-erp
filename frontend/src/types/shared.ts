export interface PersonName {
  full: string;
  short: string;
  first: string;
  last: string;
  initials: string;
  isFemale: boolean;
}

export interface CodeName {
  code: string;
  name: string;
}
