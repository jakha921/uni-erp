name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 213.230.69.57
          username: jakha
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 1892
          timeout: 600s
          script: |
            bash ~/deploy-erp.sh all 2>&1
